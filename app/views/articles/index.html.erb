<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <% @articles.each do |art| %>
        <article>
          <h2><%= link_to art.title, article_path(art) %></h2>
          <%= art.content %>
        </article>
      <% end %>
    </div>
  </div>
</div>
